
// This is the standard library so far.
// Function like the one below may require another attribute named "dyn circuit"
mod hash {
    #[foreign(sha256)]
    fn sha256(_input : []Witness) -> [2]Witness {}
}

// With structs and methods
// std::util::array_prod([x,y]) becomes [x,y].prod()

mod util {
    mod array {
        #[builtin(arraysum)]
        fn sum(_input : []Witness) -> Witness {}

        #[builtin(arrayprod)]
        fn prod(_input : []Witness) -> Witness {}
    }
}