[package]
name = "ecdsa_secp256k1_invalid_signature_in_inactive_branch"
description = "ECDSA secp256k1 verification of malformed signatures inside a branch which is never taken"
type = "bin"
authors = [""]

[dependencies]
