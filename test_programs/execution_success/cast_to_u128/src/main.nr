fn main(x: i8) {
    let y: i8 = -1;

    assert_eq(x as u128, 340282); // px: original values can't be stored in all felts
    assert_eq(y as u128, 340282); // px: original values can't be stored in all felts
}
