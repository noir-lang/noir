[package]
name = "multi_scalar_mul_invalid_scalar_in_inactive_branch"
description = "multi_scalar_mul over out-of-range scalars inside a branch which is never taken"
type = "bin"
authors = [""]

[dependencies]
