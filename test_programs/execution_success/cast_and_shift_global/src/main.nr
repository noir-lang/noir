// TODO: cleanup and re-enable previous test
global A: u32 = B;
global B: u32 = A;


// global THREE: u8 = 3;
// global EIGHT: u32 = 1 << THREE;
// global SEVEN: u32 = EIGHT - 1;

fn main() {
    // assert([0; EIGHT] == [0; 8]);
    // assert([0; SEVEN] == [0; 7]);
}
