/* Autogenerated file, do not edit! */

/* eslint-disable */
import {
  AztecAddress,
  Contract,
  ContractFunctionInteraction,
  ContractMethod,
  DeployMethod,
  Wallet,
} from '@aztec/aztec.js';
import { ContractAbi } from '@aztec/foundation/abi';
import { Fr, Point } from '@aztec/foundation/fields';
import { AztecRPC } from '@aztec/types';

import { ParentContractAbi } from '../artifacts/index.js';

/**
 * Type-safe interface for contract Parent;
 */
export class ParentContract extends Contract {
  constructor(
    /** The deployed contract's address. */
    address: AztecAddress,
    /** The wallet. */
    wallet: Wallet,
  ) {
    super(address, ParentContractAbi, wallet);
  }

  /**
   * Creates a tx to deploy a new instance of this contract.
   */
  public static deploy(rpc: AztecRPC) {
    return new DeployMethod(Point.ZERO, rpc, ParentContractAbi, Array.from(arguments).slice(1));
  }

  /**
   * Creates a tx to deploy a new instance of this contract using the specified public key to derive the address.
   */
  public static deployWithPublicKey(rpc: AztecRPC, publicKey: Point) {
    return new DeployMethod(publicKey, rpc, ParentContractAbi, Array.from(arguments).slice(2));
  }

  /**
   * Returns this contract's ABI.
   */
  public static get abi(): ContractAbi {
    return ParentContractAbi;
  }

  /** Type-safe wrappers for the public methods exposed by the contract. */
  public methods!: {
    /** enqueueCallsToPubEntryPoint(targetContract: field, targetSelector: field, targetValue: field) */
    enqueueCallsToPubEntryPoint: ((
      targetContract: Fr | bigint | number | { toField: () => Fr },
      targetSelector: Fr | bigint | number | { toField: () => Fr },
      targetValue: Fr | bigint | number | { toField: () => Fr },
    ) => ContractFunctionInteraction) &
      Pick<ContractMethod, 'selector'>;

    /** enqueueCallToChild(targetContract: field, targetSelector: field, targetValue: field) */
    enqueueCallToChild: ((
      targetContract: Fr | bigint | number | { toField: () => Fr },
      targetSelector: Fr | bigint | number | { toField: () => Fr },
      targetValue: Fr | bigint | number | { toField: () => Fr },
    ) => ContractFunctionInteraction) &
      Pick<ContractMethod, 'selector'>;

    /** enqueueCallToChildTwice(targetContract: field, targetSelector: field, targetValue: field) */
    enqueueCallToChildTwice: ((
      targetContract: Fr | bigint | number | { toField: () => Fr },
      targetSelector: Fr | bigint | number | { toField: () => Fr },
      targetValue: Fr | bigint | number | { toField: () => Fr },
    ) => ContractFunctionInteraction) &
      Pick<ContractMethod, 'selector'>;

    /** enqueueCallToPubEntryPoint(targetContract: field, targetSelector: field, targetValue: field) */
    enqueueCallToPubEntryPoint: ((
      targetContract: Fr | bigint | number | { toField: () => Fr },
      targetSelector: Fr | bigint | number | { toField: () => Fr },
      targetValue: Fr | bigint | number | { toField: () => Fr },
    ) => ContractFunctionInteraction) &
      Pick<ContractMethod, 'selector'>;

    /** entryPoint(targetContract: field, targetSelector: field) */
    entryPoint: ((
      targetContract: Fr | bigint | number | { toField: () => Fr },
      targetSelector: Fr | bigint | number | { toField: () => Fr },
    ) => ContractFunctionInteraction) &
      Pick<ContractMethod, 'selector'>;

    /** pubEntryPoint(targetContract: field, targetSelector: field, initValue: field) */
    pubEntryPoint: ((
      targetContract: Fr | bigint | number | { toField: () => Fr },
      targetSelector: Fr | bigint | number | { toField: () => Fr },
      initValue: Fr | bigint | number | { toField: () => Fr },
    ) => ContractFunctionInteraction) &
      Pick<ContractMethod, 'selector'>;

    /** pubEntryPointTwice(targetContract: field, targetSelector: field, initValue: field) */
    pubEntryPointTwice: ((
      targetContract: Fr | bigint | number | { toField: () => Fr },
      targetSelector: Fr | bigint | number | { toField: () => Fr },
      initValue: Fr | bigint | number | { toField: () => Fr },
    ) => ContractFunctionInteraction) &
      Pick<ContractMethod, 'selector'>;
  };
}
