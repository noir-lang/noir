# For running tests only, not to be depended on
# The non-test portion of barretenberg (the part you actually use) is header only
barretenberg_module(
    serialize-tests
    proof_system
    transcript
)