FROM aztecprotocol/clang:9.0.0
WORKDIR /usr/src/barretenberg
COPY . .
RUN mkdir build && cd build && cmake -DLINUX_CLANG=ON -DMEMORY_CHECKS=ON .. && make -j$(nproc)
RUN cd build && make test
ENTRYPOINT /bin/bash