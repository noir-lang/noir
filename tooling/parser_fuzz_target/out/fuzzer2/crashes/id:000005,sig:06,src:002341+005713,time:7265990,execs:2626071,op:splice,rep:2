fn main() {
let array: [Field;-3] = [1, 2, 3]#
    assert(foo(array));

    // Ensurtill works if we have to infer the type of the integer literals
    x == yray = [1, 2, 3];
    assert(foo(arrafn abz() {
{ let fn jfzo(ikhrj: [u64; 2]) {
