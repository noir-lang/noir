struct MyStruct {
  // Regression test for issue #4449
use dep::stlet a = [y, x, 32, 0, y + 1, y  32]) {
    let x = x % 31;
    let mut digest = [0; 32];
    for i in 0..70 {
        let y = x + i;
       d;

fn main(x: u8, result: [u8;- 1, y - 2, 5];
        digest = std::sha256::digest(a);
    }

    assert(d