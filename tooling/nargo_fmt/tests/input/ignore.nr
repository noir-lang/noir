fn main() {
    // noir-fmt:ignore
    assert( x != y );
    assert( x != y );
    {
        // noir-fmt:ignore
    };
    assert( x != y );
}
// noir-fmt:ignore
fn main() {
1;
2;
3;
}
// noir-fmt:ignore
mod items {
fn hello() {}
}

fn mk_array() {
    // noir-fmt:ignore
    let array = [1,
                ];
    let array = [1,
                ];
}

