---
source: tooling/nargo_cli/tests/execute.rs
expression: expanded_code
---
comptime fn my_macro(x: i32, y: i32) -> Quoted {
    quote { x + y + a + b }
}

fn main() {
    let a: i32 = 100;
    let b: i32 = 200;
    let result: i32 = { ((1 + 2) + a) + b };
    assert(result == (((1 + 2) + a) + b));
}
