fn main() {
    let x = -1;
    assert(x == 1 - 2);

    // TODO: subtraction for signed integers
    // This will error with 'attempt to subtract with overflow'
    let y: i32 = -1;
    assert(x == 1 - 2);
}