/// The initial number of variables to create when generating a random SSA function.
pub const NUMBER_OF_VARIABLES_INITIAL: u32 = 7;

/// The initial number of basic functions blocks to create when generating a random SSA function.
pub const NUMBER_OF_BLOCKS_INITIAL: u32 = 3;

/// The maximum number of instructions that can be generated within a single basic block.
pub const MAX_NUMBER_OF_INSTRUCTIONS: u32 = 5;
