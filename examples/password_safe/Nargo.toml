[package]
name = "password_safe"
type = "bin"
authors = ["Jasekeee"]
# Версию оставляем совместимой с твоим nargo 1.0.0-beta.13.
# Если у них в репо другая — мейнтейнеры поправят при ревью.
compiler_version = ">=0.19.0"
