# Function: and()

```ts
function and(lhs, rhs): string;
```

Performs a bitwise AND operation between `lhs` and `rhs`

## Parameters

| Parameter | Type |
| ------ | ------ |
| `lhs` | `string` |
| `rhs` | `string` |

## Returns

`string`
