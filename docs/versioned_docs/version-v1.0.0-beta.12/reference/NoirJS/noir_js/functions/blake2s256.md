# Function: blake2s256()

```ts
function blake2s256(inputs): Uint8Array
```

Calculates the Blake2s256 hash of the input bytes

## Parameters

| Parameter | Type |
| ------ | ------ |
| `inputs` | `Uint8Array` |

## Returns

`Uint8Array`
